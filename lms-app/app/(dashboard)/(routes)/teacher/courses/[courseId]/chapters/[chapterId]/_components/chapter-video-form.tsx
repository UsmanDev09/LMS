interface VideoFormProps {
  initialData: { videoUrl: string };
  chapterId: string;
  courseId: string;
}

const VideoForm = ({ initialData, chapterId }: VideoFormProps) => {
  return <div>Chapters Video Form Page</div>;
};

export default VideoForm;
